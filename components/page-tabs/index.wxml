<!--components/page-tabs/index.wxml-->
<view class="container">
  <view class="tab-list">
    <view class="tab" wx:for="{{tabs}}" wx:key="title">
      <view>{{item.title}}</view>
      <view wx:if="{{index===activeIndex}}" class="underline"></view>
    </view>
  </view>
  <view class="tab-content">
    <slot></slot>
  </view>
</view>